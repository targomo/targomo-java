<?xml version="1.0" encoding="UTF-8"?>
<settings>
    <servers>
        <server>
            <id>targomo-nexus-snapshots</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>targomo-nexus-releases</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>targomo</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>ossrh</id>
            <username>gerber@motionintelligence.net</username>
            <password>SONATYPE_PASSWORD</password>
        </server>
    </servers>
    <mirrors>
        <mirror>
            <id>targomo</id>
            <name>targomo</name>
            <url>${env.NEXUS_URL}/repository/maven-group/</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
    </mirrors>
    <pluginGroups>
        <pluginGroup>org.sonarsource.scanner.maven</pluginGroup>
    </pluginGroups>
    <profiles>
        <profile>
            <id>sonar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>
    </profiles>
</settings>
